{
  "name": "bolt-figma",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "devcode": "vite build --watch --config vite.config.code.ts ",
    "build": "vite build",
    "_dev": "vite build --watch",
    "_build": "rimraf dist/* && tsc ./src/code.ts --outDir ./dist && vite build",
    "__build": "rimraf dist/* && rimraf .tmp/* && vite build && vite build --config vite.config.code.ts",
    "buildmain": "vite build",
    "buildcode": "vite build --config vite.config.code.ts",
    "preview": "vite preview",
    "hmr": "vite",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.31.0",
    "@sveltejs/vite-plugin-svelte": "^2.4.2",
    "@tsconfig/svelte": "^5.0.0",
    "@types/node": "^16.7.1",
    "rimraf": "^3.0.2",
    "svelte": "^4.0.5",
    "svelte-check": "^3.4.6",
    "tslib": "^2.6.0",
    "typescript": "^5.0.2",
    "vite": "^4.4.5",
    "vite-plugin-singlefile": "^0.13.5"
  }
}
